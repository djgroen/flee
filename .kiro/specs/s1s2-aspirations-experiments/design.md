# Design Document: S1/S2 × Aspirations-Capabilities Experiments\n\n## Overview\n\nThis design document outlines the implementation of a systematic experimental framework to test dual-process cognition effects on migration decision-making through aspirations-capabilities theory. The framework builds incrementally on Flee's existing S1/S2 implementation, progressing through three phases: (1) validating current S1/S2 behavioral differences, (2) implementing aspiration-capability framework, and (3) comprehensive integration testing.\n\n## Architecture\n\n### High-Level System Architecture\n\n```mermaid\ngraph TB\n    A[Phase 1: S1/S2 Validation] --> B[Phase 2: Aspiration-Capability Framework]\n    B --> C[Phase 3: Integration Testing]\n    C --> D[South Sudan Case Study]\n    \n    E[Existing Flee S1/S2 System] --> A\n    F[Topology Generators] --> A\n    G[Experimental Infrastructure] --> A\n    \n    H[Aspiration Framework] --> B\n    I[Capability Framework] --> B\n    J[Migration Decision Logic] --> B\n    \n    K[Comprehensive Scenarios] --> C\n    L[Analysis Pipeline] --> C\n    M[Validation Tools] --> C\n```\n\n### Current Flee S1/S2 Implementation (From Flowchart Analysis)\n\n```python\n# Existing S1/S2 Logic in calculateMoveChance()\ndef check_system2_conditions(agent, time):\n    \"\"\"\n    System 2 activates when ALL conditions are met:\n    1. Conflict > 0.6\n    2. In recovery period (time >= conflict_start + 10)\n    3. Connections > 3\n    \"\"\"\n    conflict_condition = agent.location.conflict > 0.6\n    recovery_condition = time >= (agent.location.time_of_conflict + 10)\n    connection_condition = agent.attributes.get('connections', 0) > 3\n    \n    return conflict_condition and recovery_condition and connection_condition\n\ndef calculateMoveChance(agent, time):\n    if check_system2_conditions(agent, time):\n        # System 2 Active\n        provisional_route = selectRoute(agent, time, system2_active=True)\n        return 1.0, True  # Always move when S2 active\n    else:\n        # System 1 Active\n        return standard_movechance_calculation(agent), False\n```\n\n## Phase 1: Current S1/S2 System Testing\n\n### Component 1.1: S1/S2 Validation Framework\n\n**File**: `scripts/s1s2_validator.py`\n\n```python\nclass S1S2Validator:\n    def __init__(self, flee_ecosystem):\n        self.ecosystem = flee_ecosystem\n        self.s1_decisions = []\n        self.s2_decisions = []\n        \n    def track_decision(self, agent, decision_type, system2_active, factors):\n        \"\"\"Track S1 vs S2 decisions for analysis\"\"\"\n        decision_record = {\n            'agent_id': agent.id,\n            'time': factors.get('time'),\n            'system2_active': system2_active,\n            'conflict_level': agent.location.conflict,\n            'connections': agent.attributes.get('connections', 0),\n            'movechance': factors.get('movechance'),\n            'route_quality': self.calculate_route_quality(factors.get('route', []))\n        }\n        \n        if system2_active:\n            self.s2_decisions.append(decision_record)\n        else:\n            self.s1_decisions.append(decision_record)\n    \n    def analyze_behavioral_differences(self):\n        \"\"\"Compare S1 vs S2 behavioral patterns\"\"\"\n        return {\n            'movement_timing': self.compare_movement_timing(),\n            'route_quality': self.compare_route_quality(),\n            'information_utilization': self.compare_information_use(),\n            'destination_choice': self.compare_destination_patterns()\n        }\n```\n\n### Component 1.2: Topology Generator\n\n**File**: `scripts/topology_generator.py`\n\n```python\nclass TopologyGenerator:\n    def generate_linear_topology(self, n_nodes, segment_distance, pop_distribution):\n        \"\"\"Generate chain topology for testing S1/S2 in simple networks\"\"\"\n        locations = []\n        routes = []\n        \n        for i in range(n_nodes):\n            location = {\n                'name': f'Node_{i}',\n                'x': i * segment_distance,\n                'y': 0,\n                'population': pop_distribution[i] if i < len(pop_distribution) else 0,\n                'location_type': 'conflict' if i == 0 else ('camp' if i == n_nodes-1 else 'town')\n            }\n            locations.append(location)\n            \n            if i > 0:\n                route = {\n                    'name1': f'Node_{i-1}',\n                    'name2': f'Node_{i}',\n                    'distance': segment_distance\n                }\n                routes.append(route)\n        \n        return self.write_topology_files(locations, routes)\n    \n    def generate_star_topology(self, n_camps, hub_pop, camp_capacities, radius):\n        \"\"\"Generate hub-and-spoke topology for testing information centralization\"\"\"\n        # Implementation for star topology\n        pass\n    \n    def generate_grid_topology(self, rows, cols, cell_distance, connectivity_density):\n        \"\"\"Generate grid topology for testing complex network effects\"\"\"\n        # Implementation for grid topology\n        pass\n```\n\n### Component 1.3: S1/S2 Experimental Scenarios\n\n**File**: `scripts/s1s2_scenarios.py`\n\n```python\nclass S1S2Scenarios:\n    def threshold_test_scenario(self):\n        \"\"\"Test System 2 activation thresholds\"\"\"\n        return {\n            'name': 'threshold_test',\n            'topology': 'star',\n            'conflict_escalation': {\n                'start_level': 0.0,\n                'end_level': 1.0,\n                'duration': 30\n            },\n            'agent_connectivity': [0, 1, 2, 3, 4, 5, 8, 10],  # Test around threshold\n            'measurements': [\n                'system2_activation_rate',\n                'movement_timing',\n                'route_quality',\n                'destination_choice'\n            ]\n        }\n    \n    def recovery_period_test_scenario(self):\n        \"\"\"Test recovery period effects on S2 activation\"\"\"\n        return {\n            'name': 'recovery_period_test',\n            'topology': 'linear',\n            'conflict_pattern': {\n                'spike_day': 0,\n                'peak_intensity': 0.8,\n                'recovery_duration': 20\n            },\n            'measurements': [\n                'system2_activation_over_time',\n                'route_planning_quality',\n                'settlement_stability'\n            ]\n        }\n    \n    def connectivity_gradient_test_scenario(self):\n        \"\"\"Test connectivity effects on cognitive mode activation\"\"\"\n        return {\n            'name': 'connectivity_gradient_test',\n            'topology': 'grid',\n            'fixed_conflict': 0.8,\n            'connectivity_range': range(0, 11),\n            'measurements': [\n                'behavioral_threshold_detection',\n                'information_sharing_effectiveness',\n                'collective_movement_patterns'\n            ]\n        }\n```\n\n## Phase 2: Aspiration-Capability Framework\n\n### Component 2.1: Enhanced Person Class\n\n**File**: `flee/flee.py` (modifications)\n\n```python\nclass Person:\n    def __init__(self, location, attributes):\n        # ... existing initialization ...\n        \n        # Phase 2 additions: Aspiration-Capability Framework\n        self.aspirations = self._initialize_aspirations()\n        self.capabilities = self._initialize_capabilities(attributes)\n        self.migration_intention = 0.0\n        self.aspiration_history = []\n        self.capability_history = []\n        \n    def _initialize_aspirations(self):\n        \"\"\"Initialize agent aspirations based on background and context\"\"\"\n        return {\n            'safety': random.uniform(0.5, 1.0),\n            'livelihood': random.uniform(0.3, 0.9),\n            'family_reunification': random.uniform(0.2, 0.8),\n            'education': random.uniform(0.1, 0.7)\n        }\n    \n    def _initialize_capabilities(self, attributes):\n        \"\"\"Initialize agent capabilities based on attributes and random factors\"\"\"\n        return {\n            'financial': random.uniform(0, 1000),\n            'social': attributes.get('connections', 0),\n            'human_capital': random.uniform(0.3, 1.0),\n            'legal_status': random.choice(['documented', 'undocumented'])\n        }\n    \n    def update_aspirations_s1(self, information):\n        \"\"\"System 1: Fast, heuristic aspiration updates\"\"\"\n        # Simple rules: immediate response to information\n        if 'safety_threat' in information:\n            self.aspirations['safety'] = min(1.0, self.aspirations['safety'] + 0.3)\n        if 'livelihood_opportunity' in information:\n            self.aspirations['livelihood'] = min(1.0, self.aspirations['livelihood'] + 0.2)\n    \n    def update_aspirations_s2(self, information):\n        \"\"\"System 2: Analytical aspiration integration\"\"\"\n        # Complex analysis: weigh multiple factors, consider trade-offs\n        safety_weight = 0.4\n        livelihood_weight = 0.3\n        family_weight = 0.2\n        education_weight = 0.1\n        \n        # Analytical integration of new information with existing aspirations\n        # Consider reliability of information source, consistency with other info, etc.\n        pass\n    \n    def assess_aspiration_capability_match_s1(self, destination):\n        \"\"\"System 1: Simple heuristic matching\"\"\"\n        # Quick rules: \"Can I get there?\" \"Is it safe enough?\"\n        basic_safety_match = destination.safety_score >= (self.aspirations['safety'] - 0.3)\n        basic_capability_match = self.capabilities['financial'] >= destination.min_cost\n        \n        return 0.7 if (basic_safety_match and basic_capability_match) else 0.2\n    \n    def assess_aspiration_capability_match_s2(self, destination):\n        \"\"\"System 2: Comprehensive analytical matching\"\"\"\n        # Complex analysis: multi-dimensional matching with trade-off analysis\n        aspiration_scores = {\n            'safety': self.calculate_safety_match(destination),\n            'livelihood': self.calculate_livelihood_match(destination),\n            'family': self.calculate_family_match(destination),\n            'education': self.calculate_education_match(destination)\n        }\n        \n        capability_feasibility = self.assess_journey_feasibility(destination)\n        \n        # Weighted combination with capability constraints\n        weighted_aspiration_score = sum(\n            self.aspirations[asp] * aspiration_scores[asp] \n            for asp in aspiration_scores\n        ) / sum(self.aspirations.values())\n        \n        return weighted_aspiration_score * capability_feasibility\n```\n\n### Component 2.2: Migration Decision Integration\n\n**File**: `flee/moving.py` (modifications)\n\n```python\ndef calculateMoveChance(agent, time):\n    \"\"\"Enhanced with aspiration-capability logic\"\"\"\n    # Existing S1/S2 activation logic\n    system2_active = check_system2_conditions(agent, time)\n    \n    # Phase 2 addition: Aspiration-capability assessment\n    if hasattr(agent, 'aspirations'):  # Check if Phase 2 is enabled\n        # Calculate migration intention based on aspiration-capability matching\n        destinations = get_available_destinations(agent)\n        \n        if system2_active:\n            # S2: Comprehensive aspiration-capability analysis\n            best_match = max(\n                agent.assess_aspiration_capability_match_s2(dest) \n                for dest in destinations\n            )\n            agent.migration_intention = best_match\n        else:\n            # S1: Simple heuristic assessment\n            current_satisfaction = agent.assess_current_location_satisfaction()\n            if current_satisfaction < 0.3:  # Simple threshold\n                agent.migration_intention = 0.8\n            else:\n                agent.migration_intention = 0.1\n        \n        # Modify movechance based on migration intention\n        base_movechance = 1.0 if system2_active else calculate_standard_movechance(agent)\n        intention_modifier = agent.migration_intention\n        \n        return base_movechance * intention_modifier, system2_active\n    \n    # Fallback to existing logic if Phase 2 not enabled\n    return existing_calculateMoveChance(agent, time)\n```\n\n### Component 2.3: Aspiration-Capability Scenarios\n\n**File**: `scripts/aspiration_scenarios.py`\n\n```python\nclass AspirationCapabilityScenarios:\n    def aspiration_capability_mismatch_test(self):\n        \"\"\"Test whether S2 agents better match aspirations with capabilities\"\"\"\n        return {\n            'name': 'aspiration_capability_mismatch',\n            'topology': 'star',\n            'destinations': {\n                'Close_Poor': {'distance': 50, 'safety': 0.4, 'livelihood': 0.2, 'cost': 100},\n                'Medium_Good': {'distance': 150, 'safety': 0.7, 'livelihood': 0.6, 'cost': 300},\n                'Far_Excellent': {'distance': 300, 'safety': 0.9, 'livelihood': 0.8, 'cost': 600}\n            },\n            'agent_capabilities': {\n                'low_resource': {'financial': 200, 'social': 2},\n                'medium_resource': {'financial': 400, 'social': 4},\n                'high_resource': {'financial': 800, 'social': 8}\n            },\n            'measurements': [\n                'aspiration_capability_alignment',\n                'journey_success_rate',\n                'destination_satisfaction',\n                'resource_efficiency'\n            ]\n        }\n    \n    def information_cascade_test(self):\n        \"\"\"Test information utilization for aspiration formation\"\"\"\n        return {\n            'name': 'information_cascade',\n            'topology': 'tree',\n            'information_structure': {\n                'public_info': ['basic_safety', 'capacity'],\n                'network_info': ['livelihood_opportunities', 'services'],\n                'hidden_info': ['family_reunification_success', 'future_prospects']\n            },\n            'measurements': [\n                'information_discovery_rate',\n                'aspiration_accuracy',\n                'network_information_utilization'\n            ]\n        }\n```\n\n## Phase 3: Comprehensive Integration Testing\n\n### Component 3.1: Integrated Experimental Framework\n\n**File**: `scripts/integrated_experiments.py`\n\n```python\nclass IntegratedExperimentFramework:\n    def __init__(self):\n        self.s1s2_validator = S1S2Validator()\n        self.aspiration_tracker = AspirationTracker()\n        self.capability_analyzer = CapabilityAnalyzer()\n        \n    def run_comprehensive_experiment_suite(self):\n        \"\"\"Run all experiments in systematic order\"\"\"\n        results = {}\n        \n        # Phase 1: S1/S2 validation\n        results['s1s2_validation'] = self.run_s1s2_experiments()\n        \n        # Phase 2: Aspiration-capability testing\n        results['aspiration_capability'] = self.run_aspiration_experiments()\n        \n        # Phase 3: Integration testing\n        results['integration'] = self.run_integration_experiments()\n        \n        return self.generate_comprehensive_report(results)\n    \n    def run_parameter_sweep(self, parameter_ranges):\n        \"\"\"Systematic parameter space exploration\"\"\"\n        sweep_results = []\n        \n        for params in itertools.product(*parameter_ranges.values()):\n            param_dict = dict(zip(parameter_ranges.keys(), params))\n            result = self.run_single_experiment(param_dict)\n            sweep_results.append((param_dict, result))\n        \n        return self.analyze_parameter_effects(sweep_results)\n```\n\n### Component 3.2: Analysis and Validation Pipeline\n\n**File**: `scripts/analysis_pipeline.py`\n\n```python\nclass AnalysisPipeline:\n    def analyze_s1s2_differences(self, experimental_data):\n        \"\"\"Comprehensive S1 vs S2 behavioral analysis\"\"\"\n        return {\n            'movement_timing': self.analyze_movement_timing(experimental_data),\n            'route_quality': self.analyze_route_efficiency(experimental_data),\n            'information_utilization': self.analyze_information_use(experimental_data),\n            'aspiration_fulfillment': self.analyze_aspiration_outcomes(experimental_data),\n            'capability_utilization': self.analyze_resource_efficiency(experimental_data)\n        }\n    \n    def statistical_validation(self, s1_data, s2_data):\n        \"\"\"Statistical significance testing\"\"\"\n        return {\n            'mann_whitney_u': stats.mannwhitneyu(s1_data, s2_data),\n            'cohens_d': self.calculate_cohens_d(s1_data, s2_data),\n            'effect_size': self.categorize_effect_size(),\n            'confidence_intervals': self.calculate_confidence_intervals()\n        }\n    \n    def generate_publication_figures(self, analysis_results):\n        \"\"\"Create publication-ready visualizations\"\"\"\n        figures = {\n            'cognitive_activation_patterns': self.plot_s2_activation_over_time(),\n            'behavioral_differences': self.plot_s1_vs_s2_comparison(),\n            'topology_effects': self.plot_topology_interaction_effects(),\n            'parameter_sensitivity': self.plot_parameter_sweep_results()\n        }\n        return figures\n```\n\n## Data Models and Interfaces\n\n### Experimental Configuration Schema\n\n```python\n@dataclass\nclass ExperimentConfig:\n    experiment_id: str\n    phase: int  # 1, 2, or 3\n    topology_type: str\n    topology_params: dict\n    scenario_type: str\n    scenario_params: dict\n    agent_config: dict\n    simulation_params: dict\n    analysis_config: dict\n    \n@dataclass\nclass AspirationCapabilityConfig:\n    aspiration_weights: dict\n    capability_distributions: dict\n    information_sharing_rate: float\n    aspiration_update_rate: float\n    capability_learning_rate: float\n```\n\n### Output Data Formats\n\n```csv\n# s1s2_decisions.csv\ntime,agent_id,system2_active,conflict_level,connections,movechance,route_quality,destination\n\n# aspiration_evolution.csv\ntime,agent_id,safety_aspiration,livelihood_aspiration,family_aspiration,education_aspiration\n\n# capability_utilization.csv\ntime,agent_id,financial_used,social_connections_used,journey_success,resource_efficiency\n\n# migration_intentions.csv\ntime,agent_id,migration_intention,intended_destination,aspiration_capability_match\n```\n\n## Implementation Timeline\n\n### Phase 1: S1/S2 Validation (Weeks 1-2)\n- Week 1: Implement S1/S2 tracking and validation framework\n- Week 2: Create topology generators and run baseline experiments\n\n### Phase 2: Aspiration-Capability Framework (Weeks 3-4)\n- Week 3: Implement aspiration and capability attributes and logic\n- Week 4: Create aspiration-capability scenarios and initial testing\n\n### Phase 3: Integration Testing (Weeks 5-6)\n- Week 5: Implement comprehensive experimental framework\n- Week 6: Run full experimental suite and generate analysis\n\n### South Sudan Preparation (Weeks 7-8)\n- Week 7: Prepare real-world data integration capabilities\n- Week 8: Create case study framework and validation tools\n\nThis design provides a systematic, phased approach to implementing and testing the S1/S2 × aspirations-capabilities framework, building incrementally on Flee's existing cognitive system.\n"