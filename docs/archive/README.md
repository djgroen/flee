# Archive

This folder contains the original documentation as it existed before the April 2026 restructure.

- `flee/` — original flee docs (flat structure from `docs/*.md`)
- `fabflee/` — original FabFlee plugin docs (from `FabFlee/doc/`)

These files are kept for reference and should **not be edited**. They may contain outdated or inaccurate information.

The current documentation lives in the sibling folders (`getting-started/`, `concepts/`, `conflict/`, `dflee/`, `running/`, `advanced/`, `fabflee/`, `dev/`, `about/`, `code-reference/`).

This archive folder can be deleted once the new docs have been verified as complete and accurate.
