log_levels:
  agent: 0 
  link: 0 
  camp: 0
  conflict: 0 
  init: 0
  granularity: location 
spawn_rules:
  take_from_population: False
  insert_day0: True
move_rules:
  max_move_speed: 360.0
  max_walk_speed: 35.0
  foreign_weight: 1.0
  camp_weight: 1.0
  conflict_weight: 0.25
  conflict_movechance: 1.0
  camp_movechance: 0.001
  default_movechance: 0.3
  awareness_level: 1
  capacity_scaling: 1.0
  avoid_short_stints: False
  start_on_foot: False
  weight_power: 1.0
  # S1/S2 Dual-Process Decision Making Configuration
  two_system_decision_making: true  # Enable S1/S2 system (true/false)
  conflict_threshold: 0.5           # S2 activation threshold [0.0, 1.0]
  connectivity_mode: baseline        # Connectivity mode: "baseline" or "diminishing"
  steepness: 6.0                     # Sigmoid steepness parameter for S2 activation
  soft_capability: false             # Use soft capability gate (true/false)
  pmove_s2_mode: scaled              # S2 move probability mode: "scaled" or "constant"
  pmove_s2_constant: 0.9             # Constant S2 move probability (if mode is "constant")
  soft_gate_steepness: 8.0            # Steepness for soft capability gate
  # Optional: Parsimonious S1/S2 Model (alpha, beta, p_s2 only)
  # s1s2_model:
  #   enabled: false                  # Enable parsimonious model (true/false)
  #   alpha: 2.0                      # Cognitive capacity weight
  #   beta: 2.0                       # Structural opportunity weight
  #   p_s2: 0.8                       # S2 move probability
optimisations:
  hasten: 1
