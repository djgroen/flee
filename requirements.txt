networkx
matplotlib
pandas
pytest
# mpi4py must be built from source so it links against your system MPI library.
# On Cray/Archer2, first run: export MPICC=cc --no-binary=mpi4py
# On Ubuntu/Debian, first run: sudo apt install libopenmpi-dev
# On macOS, first run: brew install open-mpi
mpi4py
numpy
numpy
beartype
seaborn
muscle3
pyyaml
setuptools
# Only needed for ensemble runs with pj_type=qcg (e.g. pflee_ensemble on HPC).
# On HPC systems without internet on compute nodes, install this on the login node first.
qcg-pilotjob

